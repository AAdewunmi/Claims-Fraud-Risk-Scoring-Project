codecov:
  # Prevent status deadlocks where Codecov waits on CI/check contexts
  # that include Codecov itself.
  require_ci_to_pass: false

coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 0%
    patch:
      default:
        target: 80%
        threshold: 0%
